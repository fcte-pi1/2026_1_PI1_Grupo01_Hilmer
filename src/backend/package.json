{
  "name": "pi1-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test --test-reporter=spec",
    "coverage": "node --experimental-test-coverage --test --test-reporter=spec"
  },
  "engines": {
    "node": ">=18"
  }
}
